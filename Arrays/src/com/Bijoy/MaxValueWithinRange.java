package com.Bijoy;

public class MaxValueWithinRange {
    public static void main(String[] args) {/*
        int [] arr ={ 2,8,74,65,9,19};
                maxrange(arr,3,5);
        System.out.println(maxrange(arr));
    }

    static int maxrange(int [] arr,int start ,int end){    // Finding Maxvalue in the whole Arraylist
        int maxvalue = arr[start];
        for(int i=start; i<=end;i++){
            if(arr[i]>maxvalue){
                maxvalue= arr[i] ;
            }
        }
        return maxvalue ;*/
    }
}
